/**
 * Platform-specific color scheme hook
 * 
 * Este archivo es para plataformas nativas (iOS/Android).
 * Para web, Expo usa automáticamente use-color-scheme.web.ts
 * que maneja la hidratación de SSR correctamente.
 */
export { useColorScheme } from 'react-native';
