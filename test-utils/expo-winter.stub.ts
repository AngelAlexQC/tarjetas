/**
 * Stub para el runtime "winter" de Expo (SDK 54+).
 *
 * En unit tests no necesitamos el import.meta registry de Expo,
 * y en algunos entornos de Jest puede disparar el error:
 * "You are trying to import a file outside of the scope of the test code".
 */

export { };

