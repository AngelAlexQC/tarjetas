# Workflow principal que orquesta análisis y generación de reportes
name: SonarQube Analysis & PDF Report

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]
  workflow_dispatch:
    inputs:
      generate_pdf:
        description: 'Generate PDF report'
        required: false
        default: true
        type: boolean
      skip_tests:
        description: 'Skip tests before analysis'
        required: false
        default: false
        type: boolean

jobs:
  # Job 1: Análisis de SonarQube (reutilizable)
  sonar-analysis:
    uses: ./.github/workflows/reusable-sonar-analysis.yml
    with:
      node-version: '20'
      run-tests: ${{ github.event_name != 'workflow_dispatch' || !inputs.skip_tests }}
    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}

  # Job 2: Generación de PDF (reutilizable)
  generate-pdf:
    needs: sonar-analysis
    if: |
      github.event_name == 'push' ||
      (github.event_name == 'workflow_dispatch' && inputs.generate_pdf)
    uses: ./.github/workflows/reusable-pdf-report.yml
    with:
      project-version: ${{ github.ref_name }}
      artifact-retention-days: 30
    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
      SONAR_PROJECT_KEY: ${{ secrets.SONAR_PROJECT_KEY }}
      SONAR_PROJECT_NAME: ${{ secrets.SONAR_PROJECT_NAME }}
